{
  "name": "@agentmanifest/api-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node ../scripts/build-index.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.0.0"
  }
}
